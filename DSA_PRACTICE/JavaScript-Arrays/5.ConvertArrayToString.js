// Convert an Array to String
// We have an in-built method toString() in Javascript
// that converts an array to a string.

const courses = ["HTML", "CSS", "JavaScript"];
console.log(courses.toString());
