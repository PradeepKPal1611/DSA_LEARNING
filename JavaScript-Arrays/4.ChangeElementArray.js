// CHANGE ELEMENTS FROM A PRE-DEFINED ARRAY

// We will use index based method method to change the elements of array.

const courses = ["HTML", "CSS", "JavaScript"];
console.log(courses);
courses[1] = "GeeksForGreeks";
console.log(courses);
